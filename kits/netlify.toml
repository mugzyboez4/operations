# Functions self-route via `export const config = { path: ... }` in each .mjs file.
# No redirect rules needed — the previous /api/epic-records redirect collided with
# the function's own path config and caused 404s.
